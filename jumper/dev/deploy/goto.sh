#!/bin/bash

# =============================================================================
cd $(dirname $0)
# =============================================================================

[ ! $1 ] && echo "Usage: $0 name" && exit 1

host=$(core/deploy.js -h $1)
[ $? -ne 0 ] && echo "invalid name: $1" && exit 1

ssh game@$host
