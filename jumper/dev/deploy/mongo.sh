#!/bin/bash

# =============================================================================
cd $(dirname $0)
# =============================================================================

[ ! $1 ] && echo "Usage: $0 dbname" && exit 1

str=$(core/deploy.js -d $1)
[ $? -ne 0 ] && echo "invalid dbname: $1" && exit 1

mongo "$str"
